services:
  rest-server:
    image: restic/rest-server:latest
    container_name: rest-server-test
    environment:
      OPTIONS: "--no-auth"
    ports:
      - "8500:8000"
    volumes:
      - rest-data:/data
    restart: unless-stopped

volumes:
  rest-data:
